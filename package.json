{
  "name": "svengali-plain-vite-shoelace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler pages deploy dist",
    "deploy:test": "npx wrangler pages deploy dist --project-name=svengali-landing-staging",
    "deploy:git": "npm run build && git add . && git commit -m 'Deploy update' && git push",
    "deploy:direct": "npm run build && npx wrangler pages deploy dist --project-name=svengali-landing",
    "deploy-push": "npm run build && git add . && git commit -m \"Auto deploy\" && git push && npx wrangler pages deploy dist"
  },
  "devDependencies": {
    "vite": "^5.4.19",
    "sass": "^1.77.0",
    "vite-plugin-static-copy": "^1.0.4"
  },
  "dependencies": {
    "@shoelace-style/shoelace": "^2.15.1",
    "@splidejs/splide": "^4.1.4"
  }
}