{
  "name": "svengali-plain-vite-shoelace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npx wrangler pages deploy dist",
    "deploy:prod": "npm run build && git add . && git commit -m \"Auto deploy for production\" && git push && npx wrangler pages deploy dist",
    "deploy:staging": "npm run build && git add . && git commit -m \"Auto deploy for staging\" && git push && npx wrangler pages deploy dist --project-name=svengali-landing-staging"
  },
  "devDependencies": {
    "vite": "^5.4.19",
    "sass": "^1.77.0",
    "vite-plugin-static-copy": "^1.0.4"
  },
  "dependencies": {
    "@shoelace-style/shoelace": "^2.15.1",
    "@splidejs/splide": "^4.1.4"
  }
}